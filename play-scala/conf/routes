# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET     /*file               controllers.Assets.versioned(path="/public", file)

GET     /                                 controllers.Assets.at(path="/public", file="index.html")

POST    /seamcarvepics                         controllers.RestController.seamCarvePics

# This ended up eating too much memory, so disabled for time being
# POST    /seamcarvevideo                         controllers.RestController.seamCarveVideo